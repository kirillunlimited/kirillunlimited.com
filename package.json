{
  "name": "kirillunlimited.ru",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm-run-all clean --parallel dev:*",
    "build": "npm-run-all clean build:*",
    "deploy": "npm-run-all build deploy:*",
    "clean": "npx shx rm -rf dist",
    "dev:eleventy": "eleventy --watch --quiet",
    "dev:parcel": "parcel watch dist/tmp/**/*.html -d dist/public",
    "dev:firebase": "firebase emulators:start",
    "build:eleventy": "eleventy",
    "build:parcel": "parcel build dist/tmp/**/*.html -d dist/public",
    "deploy:firebase": "firebase deploy"
  },
  "author": "Kirill Ivanov",
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1",
    "autoprefixer": "^9.5.1",
    "npm-run-all": "^4.1.5",
    "parcel-bundler": "^1.12.3",
    "prettier": "^2.3.1"
  },
  "browserslist": [
    "last 2 versions",
    "> 1%"
  ],
  "dependencies": {
    "markdown-it": "^12.0.6",
    "markdown-it-attrs": "^4.0.0"
  }
}
