{
  "name": "kirillunlimited.ru",
  "version": "1.0.0",
  "scripts": {
    "dev": "npx shx rm -rf dist && parcel src/index.html",
    "build": "npx shx rm -rf dist && parcel build src/index.html --no-source-maps",
    "deploy": "npm run build && firebase deploy"
  },
  "author": "Kirill Ivanov",
  "devDependencies": {
    "autoprefixer": "^9.5.1",
    "parcel-bundler": "^1.12.3",
    "posthtml-include": "^1.2.0",
    "prettier": "^2.3.1"
  },
  "browserslist": [
    "last 2 versions",
    "> 1%"
  ]
}
