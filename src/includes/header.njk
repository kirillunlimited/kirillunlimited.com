<header class="header">
  <div class="header__logo">KI</div>
  <nav class="header__nav">
    <ul class="header__list">
      {%- for element in collections.pages | sortByOrder -%}
        <li>
          <a href="{{element.data.permalink}}" {% if page.url == element.url %} aria-current="page"{% endif %}>
            {{ element.data.title }}
          </a>
        </li>
      {%- endfor -%}
    </ul>
  </nav>
</header>