<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    {# Preload fonts #}
    <link rel="preload" href="{% webpack 'Rubik-Medium.woff2' %}" as="font" type="font/woff2" crossorigin>
    <link rel="preload" href="{% webpack 'Rubik-Light.woff2' %}" as="font" type="font/woff2" crossorigin>

    {# Styles #}
    <link rel="stylesheet" href="{% webpack 'dark.css' %}"/>
    <link rel="stylesheet" href="{% webpack 'og.css' %}" />
  </head>
  <body>
    {{content | safe}}
  </body>
</html>